---
"invoices": patch
---

You can now see the tax rate percentage on invoice PDFs. The summary row shows `Tax (18%)` instead of a plain `Tax` label. When order lines use different rates, a blended rate from the totals is shown; if no tax data is available, the label stays as `Tax`.
